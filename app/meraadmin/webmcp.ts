'use client';
import {useEffect} from 'react';
type Context={registerTool:(tool:{name:string;description:string;inputSchema:object;annotations:object;execute:(input:unknown)=>Promise<unknown>},options:{signal:AbortSignal})=>void|Promise<void>};
export function useRecordTools(){useEffect(()=>{const context=(document as Document & {modelContext?:Context}).modelContext;if(!context)return;const lifetime=new AbortController();void Promise.resolve(context.registerTool({name:'read_maintenance_records',description:'Read saved MeraIPO GMP and quarterly maintenance records. Requires administrator access.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},async execute(input){if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw Error('Expected an empty object');const response=await fetch('/api/admin');if(!response.ok)throw Error('Records are unavailable or access is denied');return await response.json()}},{signal:lifetime.signal})).catch(()=>{});return()=>lifetime.abort()},[])}
